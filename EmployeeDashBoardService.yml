server:
  port: 8081
message:
  greeting: Hello! EmployeeDashBoardService configuration
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
management:
  security:
    enabled: false